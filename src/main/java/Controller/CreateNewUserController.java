/*
 * Controller class for Creating a new user. However, I was unable to implement  
 * this class, since I had to focus on other functionalities.
 */
package Controller;

import java.net.URL;
import java.util.ResourceBundle;
import javafx.fxml.Initializable;
import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;

/**
 * FXML Controller class
 *
 * @author cvcol
 */
public class CreateNewUserController implements Initializable {

    static final Logger logger = LogManager.getLogger(CreateNewUserController.class);

    @Override
    public void initialize(URL url, ResourceBundle rb) {
        // Method that would have stored user information.
    }

}
